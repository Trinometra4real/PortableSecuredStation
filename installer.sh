git clone https://github.com/Trinometra4real/PortableSecuredStation.git;
mv -r ./PortableSecuredStation "/usr/lib/PSS";
cp /usr/lib/PSS/pss /usr/bin/;
sudo apt -y update;
sudo apt -y upgrade;
sudo apt -y intall python3;
sudo apt -y install pip;
sudo apt -y install pip;
pip install rsa  --break-system-package;


